Я:
- State-машина и целостность транзакций
	- Тесты
	- Целостность транзакций при операциях с файлами
	- Директории, юзеры

Настя:
- Доработка и реализация тарифных планов
- Микросервис для почтовых уведомлений (kafka...)

Паша:
- Переход на Spring JPA
	- [ ] dependency
	- [ ] Добавить JPA аннотации ко всем Entity
	- [ ] Создать конвертер для тегов, в нем вызывать FileTagsMapper
	- [ ] Создать интерфейс extends SpringJpaRepository с методами репозитория
	- [ ] Что-то настроить в кфг, если надо, для JPA
- Миграции с Flyway/LiquiBase
- Soft-delete, автоматическое очищение корзины

___
# soon...

- Контрольные суммы
- Валидация Spring
- JWT в куке
- WebMvcTest
- Хедер Location
- Общее хранилище
- Ложный `X-File-Size` при загрузке

**Рефакторинг и замечания**
- CONSTRAINT unique_file_per_parent UNIQUE (user_id, parent_id, name)
	CREATE INDEX idx_files_parent_name ON files(parent_id, name);
	CREATE INDEX idx_files_status ON files(status) WHERE status != 'READY';
- Фильтр имён файлов и прочего на наличие <, >, /
	Отсутствие в поле `name` слешей (!!!)
	Валидация на пути
- Везде передается dto в service
- Профили тестов (унаследованный application.yml)
- почему нет createdAt
- (опц) отрисовка списка файлов на фронте только по запросу getFileList
- Fast check не срабатывает при пуше не в main (тогда зачем он?)
- Призрачные записи остались? Хотя есть же транзакции... мб идемпотентность addFile и deleteFile не нужна или неправильна?
- Мб добавить таймут в вирт. потоке ChunkedHttpHandler (не только по разрыву соединения)
- Больше логов для state-машины
- FileService - God Class
- Оркестрация сервисов
- Проверить все `throw new`
- Правильный хедер для `Authorization`
- Watermarks, channel.isWritable против перенасыщения буфера клиента

**Ошибки**
- Из старого пиара
- Зачем UserSessionService, если есть JWT токены? Нарушение принципа REST
- Неверное удаление (сейчас рекурсивно ТОЛЬКО в Postgres)
- Пустой `catch` в `checkStorageAndNotify`
- Хардкодинг в `docker.compose.yml`

**Тесты**
- Изменение parentId (особенно в директориях)
- Невалидный путь при создании

**Фронт**
- Чанковая загрузка
- parentId + name / fileId

**(!!!) Изменить описание ручек**