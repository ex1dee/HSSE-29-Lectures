Переменная $x_i$ функции $f(x_1, \dots, x_n)$ называется **существенной**, если найдется такой набор значений остальных переменных, при котором изменение $x_i$ меняет значение всей функции:

$$f(\dots, 0, \dots) \neq f(\dots, 1, \dots)$$

В противном случае переменная называется **фиктивной**.