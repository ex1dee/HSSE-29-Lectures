 Метрическое пространство $(x, d)$ называется свзяным, если его нельзя представить как объединение непустых $X_{1}$ и $X_{2}$, где $X_{1}$ - открытое.
В обратном случае называется **несвязным**.

Пример. $X = \beta_{1}(0, 0) \cup \beta_{2} (0, 5)$
$d$ - метрика, индуцированная из $(\mathbb{R}^2, ||*||_{2})$


Определение связности <=> существует в метрическом пространстве и хотя бы является непустым множеством, открытым и замкнутым одновременно.


Метрическое пространство называется линейно свзяным, если любые две точки можно соединить кривой.

Линейная связность => топологическая связность
Топологическая связность НЕ => линейная связность

Пусть $(X, d)$ - линейно связное.
Предположим, оно не топологически связное (это объединение непустых открытых множеств)
Возьмём две точки (по одной в каждом из "кусков")
Можем ли мы найти кривую с концами в этих точках?
Предположим, что $\ \exists \gamma in C([a, b], X)$, такое, что $\gamma(a)=x_{1}, \gamma(b)=x_{2}$
$\gamma([a, b])=\gamma([a, b]) \cap X_{1} \cup \gamma([a, b]) \cup X_{2}$

$E_{1} := {t \in [a, b]: \gamma(t) \in X_{1}} = \gamma^{-1}(X_{_{1}})$ - открытое в $[a, b]$
$E_{2} := {t \in [a, b]: \gamma(t) \in X_{2}} = \gamma^{-1}(X_{_{2}})$ - открытое в $[a, b]$

$[a, b] = E_{1} \cup E_{2}$ представимо как объединение двух относительно открытых множеств, что невозможно.

$t^*_{1} = sup({t \in [a,b]: \gamma(t)in X_{1}})$
$t^*_{2} = inf(t \in [a,b]: \gamma(t)in X_{2})$
Этим двум точкам нельзя ничего сопоставить в образе! 
$[t^*_{1}, t^*_{2}]$ НЕ в $\gamma^{-1}(X_{1} \cup X_{2})$

Пример топологически связного, но не являющегося линейно связным.
Рассмотрим $f(x)= \sin\left( \frac{1}{x} \right), x in (0, 1]$
$f(x)=0, x=0$
$Graph f := {(x, f(x))} \in \mathbb{R}^2, x in [0, 1]$
$E_{2} = {(0, y) \in \mathbb{R}^2: y \in [-1, 1]}$
$X = E_{1} \cup E_{2}$ с индуцированной из $\mathbb{R}^2$ метрикой

Теорема.
$(X, d)$ и $(Y, p)$ - метрические пространства.
$f: X \to Y$
Следующие условия равносильны:
1) f непрерывно на X
2) $\ \forall V in Y$ - открытое $f^{-1}(Y)$ - открытое в X

2 => 1
2 => $f^{-1}(\beta_{eps}(y))$ - открытое 
$x = f^{-1}(y)$
$\implies \ \exists d(x, t) > 0$, то есть $B d(x)(X) \in f^{-1}(\beta_{eps}(Y))$
=> $f(\beta_{d(x)}(X)) \in \beta_{e}(f(x))$
=> $\ \forall eps > 0 \ \exists d(x, e)>0: \ \forall x' \in \beta_{d(x, e)} \to f$ непр. в X

1 => 2
Пусть $V in Y$ - произвольное открытое множество.
Если $f^{-1}(V)=empty$, то доказывать нечего.

Если же $f^{-1}(V) not empty$, пусть $\ \forall y in V$ , такая, что $f^{-1}(y) not empty$
Так как $\ \forall$ открытое, то $\ \exists eps>0$, т. ч. $\beta_{eps}(y) in V$

В силу непрерывности, $\ \exists d(eps, x)>0, т. ч. f(\beta_{d(eps, x)}(x)) in \beta_{eps}(y)$
=> $\beta_{d(eps, x)}(x) in f^{-1}(\beta_{eps(y)})$


В итоге $\forall x in f^{-1}(V)  \exists d(x, eps)>0$, т. ч. $\beta_{d(eps, x)}(x) \in f^{-1}(\beta(y)) \in f^{-1}(Y)$
$\implies f^{-1}(V)$ - открытое в X множество

Продвинутая теорема Больцано-Коши.
Пусть $(X, d) - множество, связное$ метрическое пространство.
и $f: X\to \mathbb{R}$ метрическое в каждой точке.
Тогда $\ \forall A \in \mathrm{Im}F(A<B)$
$\ \forall B in \mathrm{Im}f$
$\ \forall C \in [A, B] \implies \ \exists x_{c} in X$, т. ч. $f(x_{c}) in C$

Пусть $A \in \mathrm{Im}f \implies \ \exists x_{1} in X$, т. ч. $f(x_{n})=A$
$B \in im f \implies \ \exists x_{2} in X$, т. ч. $f(x_{2})=B$
и $A < C < b$
Так как X - линейно связная => $\ \exists \gamma$ кривая, непрерывная и связывающая $X_{A}$ и $X_{B}$ <=> ${\gamma} in C ([a, b], X) \gamma(0)= x_{1}$ и $\gamma(1)=x_{2}$
=> по теореме Больцано-Коши о промежуточном значении
$\ \exists t_{c} in [0, 1]$, т. ч. $h(t_{c})$=C= $f(\gamma(t_{2}))$
$X_{c} := \gamma(t_{c})$
=> $f(x_{c})=C$, что и требовалось доказать.

Теорема о непрерывности композиции.
Пусть $(X_{c}), d_{1}, (X_{2}, d_{2}), (X_{3}, d_{3})$
Тогда  $h := g * f \in C (x_{1}, x_{3})$

Доказательство.
В силу эквивалетности характерной непрерывности достаточно доказать то, что $\ \forall V \in X_{3} \to h^{-1}(V)$ - открытое в $X_{1}$
$V$ - открытое

$h^{-1}(V) = (g*f)^{-1}(V) = f^{-1}(g^{-1}(V)$ = открытое множество в $X_{1}$
Доказано.




Пусть $(X, d)$ - метрическое пространство.
Пусть $S \in X$, $S \neq 0$
Тогда $S$ само можно рассмотреть как метрическое пространство с индуцированной метрикой ds
$\ \forall x, y \in S ds(x,y)=d(x,y)$ 

Множество V называют относительно открытым в S, если оно как подмножество метрического пространства (S, ds)
Пример. $X = \mathbb{R}$
$V = [0, \frac{1}{2})$
$S=[0, 1]$
V относительно открыто в S, но не открыто в X

Множвество $V \in S$ является относительно открытым в $S$
<=> $\ \exists W$ - открытое в X, такое, что $V = W \cap S$

Доказательство.
<= 
Пусть x - произвольная точка из пересечения. 
$\ \exists d>0$, такое, что $\beta_{d(x)} \in W$ => $\beta_{d(x)} \cap S \in V$
$\beta_{d(x)} \cap S$ - шар в пространстве S, относительно индуцированной метрики.


=> 
Пусть $V \in S$ - относительно открытое множество.
Покажем, что его можно реализовать как пересечение $V \in U \cap S$
Где U - открытое в X

Фиксируем $x \in V$
Так как W относительно открыто, то $\ \exists d>0$, такое, что $\beta s_{d(x)} \in V$

Но $x \in V$ произвольно.
$U := U_{x \in V} \beta_{d_{x}}(x)$ - открытое в X.
$U \cap S = (U_{x \in V}) \beta_{e_{x}}(x) \cap S = U_{x \in V} \beta_{e_{x}} \cap S \in V$
С другой стороны $V \in U_{x \in V} \beta_{e_{x}}(x) \cap S \in U \cap S$
=> $V = U \cap S$
Доказано.

Следствие. 
$(X, d)$ и $(Y, d)$ - метрические пространства
$E \in X$ $G \in Y$
Пусть $f: E \to G$ - метрические пространства в каждой точке множества E  по множеству E <=> $f \in C(E, G)$
Если E и G рассмотреть как метрические пространства (подпространства в X и Y соответственно)


Предел по направлению и предел по совокупности переменных.
$f: \beta_{d}(x) \to \mathbb{R}$
($\beta$ проколотое, лежит в $\mathbb{R}^n$)

$\lim_{ x \to x_{0} }$ $A \in \mathbb{R}$ по совокупности переменных ($\mathbb{R}$ с чертой)
$\ \forall eps>0 \ \exists d \in (0, d]: \ \forall x \in \beta_{d}(x_{0}) \implies f(x) \in U_{eps}(A)$
$U_{eps}$ - эпсилон-окрестность.


