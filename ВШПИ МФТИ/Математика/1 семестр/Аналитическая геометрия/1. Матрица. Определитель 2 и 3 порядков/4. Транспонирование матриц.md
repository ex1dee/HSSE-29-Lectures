$A^T_{m,n}$ — матрица, полученная путём **транспонирования** матрицы $A_{n,m}$, у которой
$$
(A^T)_{ij} = A_{ji}
$$
То есть строки и столбцы меняются местами

___

#### Основные свойства

1. $(A^T)^T = A$
2. $(A + B)^T = A^T + B^T$
3. $(\lambda A)^T = \lambda A^T$
4. $(AB)^T = B^TA^T$