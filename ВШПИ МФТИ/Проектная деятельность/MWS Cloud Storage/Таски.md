# soon...

- **Retry**, FailSafe

___

Я
- Рефакторинг
	- Метаинфа о состоянии файлов и ошибках в JSON `Postgres`
	- `JavaDoc` для entities
	- intergration тесты при пуше не в main?
	- `uses` в CI дублируется
	- handleScaleFile -> handleStaleFile
	- spring 3.4.3 -> 3.5.11
	- Нейминг в `StorageRepositoryWrapper`
	- Переделать исключения `checked` в `unchecked`
	- Слишком громоздкий блок `catch`:

	``` java
	} catch (StorageFileAlreadyExistsException  
	    | UserNotFoundException  
	    | UploadSessionNotFoundException  
	    | HeaderNotFoundException  
	    | TransferAlreadyStartedException  
	    | TransferNotStartedYetException  
	    | StorageEntityNotFoundException  
	    | TooSmallFilePartException  
	    | ValidationFailedException  
	    | StorageIllegalAccessException  
	    | QueryParameterNotFoundException e) {  
	  ResponseUtils.sendBadRequestExceptionResponse(ctx, e);
	}
	```
- State-машина
	- Тесты
	- Переход с `ERROR` на `FATAL`
	- Целостность транзакций при операциях с файлами

Настя:
- Реализация тарифных планов
- Отдельный сервис-приложение для уведомлений

Паша:
- Spring JPA