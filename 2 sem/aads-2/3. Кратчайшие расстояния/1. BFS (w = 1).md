**Асимптотика:** $O(V + E)$.

```cpp
// dist = [-1] * n
dist[start] = 0;
queue.push(start);
while (!queue.empty()) {
    v = queue.front(); queue.pop();
    for (u : graph[v]) {
        if (dist[u] == -1) {
            dist[u] = dist[v] + 1;
            queue.push(u);
        }
    }
}
```