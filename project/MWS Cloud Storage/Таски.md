Настя:
- Уровни доступа к хранилищу (мб семьи)
- Фронт: корзина, возобновление чанковой передачи, parentId+name

Паша:
- Переход на SeaweedFS
- Тесты на soft/hard delete и state-машину
- Health checks

Сергей:
- Рефакторинг: валидация, Jackson и прочее
- Безопасность: защита от инъекций, обязательный редирект на HTTPS, динамическая валидация размера файлов при чанковой
- Контрольные суммы для загрузки
- *+ Разрывы соединения при чанковой загрузке*

Общее:
- Продумать внедрение и принцип работы сканирования на вирусы

Дополнительно:
- Пагинация
- Метрики

___
# soon...

**RequestUtils больше не нужен? ResponseUtils? FileTagsMapper?**
Ручки изменились!
И в пароле: 
```
Password must be between 8 and 64 characters long and include uppercase and lowercase letters, digits, and special characters (@#$%^&+=!_-)
```

- Посмотреть проект по уведам
- Проверка на кодстайл (засорение коммитов, не у всех срабатывает)
- CDC + Outbox + Debesium
- Сортировка файлов
- Хедер Location
- failFast при чанковой загрузке на фронте
- Отмена загрузки
- Access Denied проверки (тарифы, чанковая загрузка, остальные операции)
- Сделать корзину плоской
- Автомат. очистка upload сессий + очистка при удалении файла

**Вопросы**
- Везде ли верно стоит is_deleted в sql запросах?
- А если соединение при чанковой загрузке разорвётся?
- В чем недостатки виртуальных потоков?
- Что, если истек тариф 30 гб, а по дефолту 10гб?
- Почему бы не поставить @Transactional(timeout=...) над uploadFile?
- if (!tariffService.hasAccess(request.userId())) { в startChunkedUpload?
- Нужен ли log.isDebugEnabled?
- Зачем на каждый канал создавать SslContext?

**Ошибки**
- Удалить авто чекинг перед коммитом
- В проекте по уведам нет обработчика ошибок + огромный switch-case блок в  sendNotification + странный getSubjectForDaysLeft + `String.format("%.1f%%", percentUsed)` вместо вызова FormatUtils в notifyStorageAlmostFull
- ЛОВИТЬ FailsafeException
- Хардкодинг в `docker.compose.yml`
- А если `getFile` во время `hard delete`?
- Проверка на вместимость хранилища
- А если ошибка произошла в сервисе, но не во враппере? (статус ERROR)
- В `StorageRepositoryWrapper` ловится Exception, но нет проверки на InterruptedException
- Нигде не происходит вызов activateUser

**Тесты**
- WebMvcTest
- Изменение parentId (особенно в директориях)
- Невалидный путь при создании
- Soft/hard delete
- И другие новые методы

**Фронт**
- Кириллица в хедерах

**Рефакторинг и замечания**
- **База данных и миграции**
	    
	- Добавить поле createdAt — добавить аудит времени создания записи (сейчас только updatedAt).
	
- **Безопасность и валидация**
	
	- Валидация путей — проверка корректности структуры при перемещении и создании папок.
	    
	- Правильный хедер для Authorization — переход на стандартный заголовок `Authorization: Bearer <token>` вместо кастомного

- **Архитектура**
	
	- FileService (God Class) — разделение ответственности, оркестрация сервисов, требуется рефакторинг
	    
	- Профили тестов — настройка иерархии `application.yml` для разделения конфигураций dev, test и prod сред.
	    
	- Асинхронное удаление файлов (воркеры)
	
- **Сетевой слой (Netty & API)**
	
	- Перепутаны PUT и POST — приведение методов к стандартам REST
	    
	- Зачем X-File-New-Visibility в хедерах 
	    
	- X-File-Size vs ContentLength
	    
	- Тайм-аут в виртуальном потоке ChunkedHttpHandler — принудительное завершение потока

	- При очистке в StaleFileCleanupScheduler нет удаления из activeUploads
	    
	- Watermarks и channel.isWritable — внедрение механизмов контроля потока (Backpressure), чтобы не переполнять буфер клиента при скачивании.
	    
	- Маппинг HTTP статусов в исключениях
	
	- Тяжелые вычисления в Event Loop
	
- **CI/CD и Фронтенд**
	
	- Изменение срабатывания Fast check 
	    
	- Загрузка данных на фронте только при вызове `getFileList`
	
- **Прочее**
	- Больше (и меньше) логов
	- Хардкодинг в TariffPlan
	- LocalDateTime vs Instant vs OffsetDateTime
	- Кроны вынести в кфг
	- BaseStorageException vs BaseException
	- Обработка ошибок при отправке уведомлений
	- storageLimit больше не нужен
	- В ApiEndpoints еще указывать метод