1. Minio теперь синхронный
2. split file path to displayed path (that user specify) and S3 key (ownerId/fileId)
3. объединение частей в FileService (uploadPart)
4. Пытался делать с встроенным в Java java.net.http.HttpClient, но чанковую загрузку он не поддерживает. Вместо него в чанковой передаче использую Apache HttpClient