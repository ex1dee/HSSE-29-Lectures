POST   /api/users/auth/login          # Вход в систему
- **X-Auth-Email**
- **X-Auth-Password**

___

GET   /api/users/info          # Инфо о юзере
- **X-Auth-Token**

___

POST   /api/users/update          #  Изменение данных о юере
- **X-Auth-Token**
- **X-New-Username** (опционально)
- **X-Old-Password** (опционально)
- **X-New-Password** (опционально)

___

POST   /api/users/auth/login          # Вход в систему
- **X-Auth-Email**
- **X-Auth-Password**

___

POST   /api/users/auth/register       # Регистрация нового пользователя
- **X-Auth-Email**
- **X-Auth-Password**
- **X-Auth-Username**

___

POST   /api/users/auth/logout         # Выход
- **X-Auth-Token**

___

+GET    /api/files?directory={directoryToSearch}               # Получить список файлов (с пагинацией)
- **X-Auth-Token**

*Параметр directory пока не учитывается*

___

GET    /api/files?path={filePath}      # Скачать файл (chunked)
- **Transfer-Encoding:** chunked
- **X-Auth-Token**

___

+GET    /api/files?path={filePath}      # Скачать файл
- **X-Auth-Token**

___

POST   /api/files/upload?path={filePath}        # Загрузить файл (chunked)
- **Transfer-Encoding**: chunked
- **X-Auth-Token**
- **X-File-Tags**
- **X-Progress-Update**

___

+POST   /api/files/upload?path={filePath}        # Загрузить файл
- **X-Auth-Token**
- **X-File-Tags**

___

DELETE /api/files?path={filePath}      # Удалить файл
- **X-Auth-Token**

___

+PUT    /api/files?path={filePath}      # Переименовать/обновить метаданные
- **X-Auth-Token**
- **X-File-New-Path** (опционально)
- **X-File-Visibility** (опционально)
- **X-File-Tags** (опционально)

___

+GET    /api/files/info?path={filePath} # Получить метаданные файла
- **X-Auth-Token**

___

POST   /api/folders?folder={folderPath}              # Создать папку
- **X-Auth-Token**

___

POST    /api/folders?from={source}}&to={dest}  # Изменить папку
- **X-Auth-Token**

___

DELETE /api/folders?folder={folderPath}   # Удалить папку
- **X-Auth-Token**

___

??? GET    /api/storage/quota        # Информация о квотах
??? GET /api/health